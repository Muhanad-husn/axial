# Axial — Engineering Handbook

> Placeholder. The full handbook (role boundaries, merge authority, spec-freeze rules,
> the behavior-first loop, and the two hard gates) is written in Phase 1 of the
> agentic-engineering-org setup. Until then: all work happens on branches; only the
> founder approves merges to `main`.

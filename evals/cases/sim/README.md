# evals/cases/sim/ — SIMULATED hard cases (throwaway)

`origin: simulated`. AI-generated stand-in answer-quality referee cases, **not**
academic-authored (those land in [`../`](../)). They pin a **provisional** hard-case
format (PHASE-B.md §9 / Open Question on adjudication format) and exist to dry-run the
rung-3 gates during the academic pause. Deleted and re-run on real academic input
before any trusted number. See [`docs/sim-academic/`](../../../docs/sim-academic/) and
`docs/DECISIONS.md` DEC-29.

Each `*.json` references sources and chunks by id only (safe to commit per DEC-23).

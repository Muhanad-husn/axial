# The two tests this slice adds, run on their own

$ uv run pytest src/axial/test_cli_ask.py -v -k 'fork'
============================= test session starts =============================
platform win32 -- Python 3.13.14, pytest-9.1.1, pluggy-1.6.0
rootdir: D:\axial
configfile: pyproject.toml
plugins: anyio-4.14.1, Faker-40.28.1, xdist-3.8.0
created: 8/8 workers
8 workers [2 items]

..                                                                       [100%]
============================= 2 passed in 24.11s ==============================

# The diff

$ git diff 3fb17cc..HEAD -- src/axial/cli.py
diff --git a/src/axial/cli.py b/src/axial/cli.py
index 69acb3e..905860e 100644
--- a/src/axial/cli.py
+++ b/src/axial/cli.py
@@ -2514,7 +2514,13 @@ def _ask(
                 previous=previous,
                 weights=weights,
                 on_event=_print_event,
-                on_fork=_fork_prompt,
+                # One-shot (both `question` and `case` given up front) is
+                # this docstring's own promise of "no prompts": `on_fork=
+                # None` is `ask()`'s documented way of declining an
+                # interactive answer, so a genuine fork is recorded
+                # unanswered (`intake_fork.answer: null`) instead of
+                # blocking on `input()` with no stdin to read (#790).
+                on_fork=None if one_shot else _fork_prompt,
             )
         except _ASK_ERRORS as exc:
             print(f"error: {exc}", file=sys.stderr)

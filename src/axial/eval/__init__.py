"""Eval instruments: `corpus_pin` (the corpus-pin manifest), `cases` and
`classification` (answer-quality eval support). The gold-set scoring harness
that used to live here (joining `data/gold/chunks/*.json` against the
Academic's returned `label_sheet.xlsx`) was retired in #710, along with the
gold set itself."""

# Copy to secrets/secrets.toml (gitignored) and fill in real values.
# Never commit the real file.

[openrouter]
api_key = "sk-or-..."

[nvidia]
api_key = "nvapi-..."

[google_drive]
# Service-account JSON path or OAuth client config, per src/axial/drive setup.
credentials_path = "secrets/gdrive-credentials.json"
